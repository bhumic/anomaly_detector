<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Show anonymous pages data, should always be in the overflow -->
    <item android:id="@+id/show_anon_pages"
          android:title="@string/show_anon_pages"
          android:showAsAction="never" />
</menu>